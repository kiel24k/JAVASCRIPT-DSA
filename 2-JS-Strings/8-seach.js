let text = "Kiel, Alarcon,  Bermudez"

// The search() method searches a string for a value. the method returns the index (position) of a match. It returns -1 if no match is found. The search() method is case sensitive.

let result = text.search("Alarcon")
console.log(result);
